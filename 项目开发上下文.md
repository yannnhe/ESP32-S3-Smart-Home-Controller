# ESP32-S3 智能家居项目开发上下文

> 新会话开始时请先阅读本文件，再检查 `git status` 和相关源码。本文不记录任何 Wi-Fi/MQTT 密码。

## 1. 项目目标

- 将本科毕业设计从 ESPHome YAML 逐步迁移到原生 ESP-IDF。
- 目标芯片：ESP32-S3；当前 ESP-IDF：6.0.2。
- 通过 Wi-Fi 接入树莓派 4B 上的 HAOS，并使用 Mosquitto MQTT 与 Home Assistant 通信。
- 后续逐步移植传感器、执行器、自动化、本地 UI、存储和 OTA。

## 2. 当前目录结构

```text
homeassistant/
├── main/                    # app_main 与系统初始化编排
├── components/
│   ├── network/             # 已完成：Wi-Fi STA 网络层
│   ├── ha_mqtt/             # 已完成基础层：MQTT/HA 通信
│   ├── bsp/                 # 仅有组件骨架
│   ├── sensors/             # 待开发
│   ├── actuators/           # 待开发
│   ├── automation/          # 待开发
│   ├── ota_service/         # 待开发
│   ├── storage/             # 待开发
│   ├── local_ui/            # 待开发
│   └── app_config/          # 待开发
├── yaml/                    # 原 ESPHome 配置，仅作迁移参考，可能含敏感信息
├── managed_components/      # 自动下载的 DHT、辅助库、ESP-MQTT；勿手改
├── build/                   # 构建产物
├── dependencies.lock        # 托管组件版本锁定
└── partitions-16MiB.csv     # 预备分区表（当前是否启用需核查 sdkconfig）
```

## 3. 已完成功能

### Network

- 固定 SSID/密码从 Kconfig 读取，STA 模式、DHCP IPv4。
- WPA2/WPA3 认证选项；非阻塞启动。
- 断线后使用 `esp_timer` 指数退避重连（1 秒起，默认最大 30 秒）。
- 发布 `NETWORK_EVENT_CONNECTED/DISCONNECTED`。
- 提供 IP、RSSI、连接状态、手动重连、停止等接口。
- 当前关闭 Wi-Fi 省电模式，优先保证 MQTT/OTA 稳定性。

### MQTT/HA 基础通信层

- Broker 默认 URI：`mqtt://192.168.31.163:1883`；用户名/密码只从 Kconfig 读取，默认空。
- MQTT 3.1.1、Clean Session、Keep Alive 60 秒、QoS 0/1 通用文本发布接口。
- Client ID：`esp32-1-<STA MAC 后六位>`。
- Wi-Fi 获得 IP 后启动 MQTT；ESP-MQTT 负责 Broker 自动重连。
- availability：`smarthome/esp32-1/status`；连接后 retained `online`，异常掉线由 retained LWT `offline`。
- 订阅 `homeassistant/status`；收到 `online` 时先重发设备 online，再发布内部事件。
- 对外事件：
  - `HA_MQTT_EVENT_CONNECTED`
  - `HA_MQTT_EVENT_DISCONNECTED`
  - `HA_MQTT_EVENT_HOME_ASSISTANT_ONLINE`
- 已验证过 MQTT Discovery 测试开关，现已按要求彻底删除；当前不发布任何实体 Discovery。
- 最近一次 ESP-IDF 完整构建成功；未烧录本次删除测试开关后的固件。

## 4. 正在开发的功能

- MQTT 基础层已稳定，下一阶段准备移植真实传感器和控制功能。
- 尚未实现通用 MQTT 订阅/命令路由；应在第一个需要接收命令的执行器落地时再设计。
- 各业务组件未来需监听 MQTT 事件，自行发布自己的 Discovery 与状态。

## 5. 关键技术

- ESP-IDF 默认事件循环用于组件解耦：
  - `network` 将底层 Wi-Fi/IP 事件转换为 `NETWORK_EVENT`。
  - `ha_mqtt` 监听网络事件，并将 MQTT/HA 状态转换为 `HA_MQTT_EVENT`。
- Home Assistant MQTT Discovery：传统方案为“每个 HA 实体一条独立 config JSON”。
- 业务组件在以下两种事件后重新发布 Discovery 和当前状态：
  - MQTT 连接/重连：`HA_MQTT_EVENT_CONNECTED`
  - HA 自身重启上线：`HA_MQTT_EVENT_HOME_ASSISTANT_ONLINE`
- 状态通常应 retained；控制命令通常不 retained；关键消息推荐 QoS 1。
- 托管依赖：`esp-idf-lib/dht 1.2.0`、其 helpers `1.4.0`、`espressif/mqtt 1.1.0`。

## 6. 重要文件说明

- `main/main.c`：NVS、netif、默认事件循环、MQTT 与 network 初始化顺序。
- `components/network/network.c`：STA 状态机、Wi-Fi/IP 回调、重连定时器。
- `components/network/include/network.h`：network 公共 API 与事件。
- `components/network/Kconfig.projbuild`：SSID、密码、连接超时、安全模式、最大退避时间。
- `components/ha_mqtt/ha_mqtt.c`：ESP-MQTT 生命周期、HA 状态订阅、availability/LWT、通用发布。
- `components/ha_mqtt/include/ha_mqtt.h`：MQTT 公共 API 与三个高级事件。
- `components/ha_mqtt/Kconfig.projbuild`：Broker、用户名、密码、Keep Alive。
- `components/ha_mqtt/idf_component.yml`：`espressif/mqtt` 依赖。
- `main/idf_component.yml`：DHT 依赖；当前还重复声明了 MQTT，可后续只保留 `ha_mqtt` 中的声明。
- `dependencies.lock`：准确锁定托管依赖版本，应提交；`managed_components/` 可重建且已忽略。

## 7. 已知问题

- Mosquitto 可能仍保留旧测试开关的 retained Discovery/状态，新固件不会主动删除；需手动清理：
  - `homeassistant/switch/esp32_1_mqtt_test_switch/config`
  - `smarthome/esp32-1/switch/mqtt_test/state`
- 1883 是未加密 MQTT；正式部署后考虑迁移到 8883 TLS 并校验证书。
- `ha_mqtt` 暂无 stop/deinit、二进制发布、QoS 2、大 Payload 分片重组和通用订阅路由。
- `main/idf_component.yml` 与 `components/ha_mqtt/idf_component.yml` 重复声明 MQTT。
- 构建曾有一次 GCC 在 ESP-IDF 官方 `esp_lcd_panel_rgb.c` 中发生内部段错误，直接重试成功，与项目 MQTT 代码无关。
- 工作区存在未提交修改；不要覆盖或回退现有 `network.c`、`main.c` 等用户改动。

## 8. 下一步要做什么

1. 从原 YAML 选择第一个真实传感器，明确型号、GPIO、采样周期、量程和 HA 实体属性。
2. 在 `components/sensors/` 中按设备建立驱动/业务组件，不把传感器逻辑写进 `ha_mqtt.c`。
3. 为每个 HA 实体定义独立 Discovery topic/JSON 和 state topic。
4. 监听 `HA_MQTT_EVENT_CONNECTED` 与 `HA_MQTT_EVENT_HOME_ASSISTANT_ONLINE`，重发 Discovery/状态。
5. 使用 `ha_mqtt_publish()` 发布数据并完成 HA 联调。
6. 开始第一个执行器时，再设计通用订阅/命令路由与非阻塞硬件执行机制。
7. 之后实现 OTA；正式存储业务数据前重新评估 `main.c` 中 NVS 整分区擦除恢复策略。

## 9. 不能改动/需要注意的约束

- 不硬编码或输出 Wi-Fi/MQTT 密码；凭据仅由 menuconfig 写入本地 `sdkconfig`。
- 不提交 `sdkconfig`、`managed_components/`、`build/` 或含凭据的 YAML；`.gitignore` 已配置。
- 不直接修改 `managed_components` 源码；依赖应通过 `idf_component.yml` 管理。
- 保持组件分层：`network` 不包含 MQTT 业务，`ha_mqtt` 不包含具体传感器/执行器逻辑。
- MQTT 回调和 ESP-IDF 事件回调中不要执行阻塞式硬件操作。
- Wi-Fi 掉线时不销毁 MQTT Client；由 network 恢复 Wi-Fi、ESP-MQTT 自动恢复 Broker 会话。
- 当前保留设备 availability/LWT 和 `homeassistant/status` 订阅。
- 当前暂不实现通用订阅路由，不恢复已删除的测试开关。
- 操作前先检查 `git status`；保留所有不属于当前任务的现有修改。
- 构建环境是 Windows/PowerShell；普通终端可能没有 `idf.py`，优先使用 VS Code ESP-IDF 专用终端。
