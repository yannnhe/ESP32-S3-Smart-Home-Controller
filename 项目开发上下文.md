# ESP32-S3 智能家居项目开发上下文

> 新会话先完整阅读本文，再检查 `git status` 和相关源码；未获明确授权前只理解与汇报，不开发、不修改、不提交。本文不记录 Wi-Fi/MQTT 凭据。

## 1. 项目目标

- 将 ESPHome 配置逐步迁移为 ESP32-S3（ESP-IDF 6.0.2）原生组件。
- 设备通过 Wi-Fi、Mosquitto MQTT 接入 Home Assistant；HA 实体使用 MQTT Discovery。
- 已取消的范围不再迁移；后续重点是其余真实传感器、窗户舵机、OTA 等。

## 2. 当前目录结构

```text
main/                 启动与初始化编排
components/
  network/            Wi-Fi STA 与重连
  ha_mqtt/            MQTT、LWT、availability、HA 事件
  ha_discovery/       Discovery 注册表和专用发布任务
  sensors/            三个 DHT11
  diagnostics/        Wi-Fi RSSI
  actuators/          待实现（后续窗户舵机等）
  bsp/ storage/ ota_service/ ...  待完善
yaml/esp32-1.yaml     保留的 ESPHome 迁移参考，可能含凭据
managed_components/   托管依赖，禁止手改
build/                构建产物
```

## 3. 已完成功能

- `network`：STA、DHCP、指数退避重连、`NETWORK_EVENT_CONNECTED/DISCONNECTED`、IP/RSSI 查询；关闭省电模式。
- `ha_mqtt`：Wi-Fi 获得 IP 后启动 MQTT、自动重连、retained LWT/availability（`smarthome/esp32-1/status`）、订阅 `homeassistant/status`，并发布 MQTT/HA 高层事件。
- `ha_discovery`：所有实体归入 `esp32-1` Device；Discovery 与状态均为 retained QoS 1。
- DHT11：客厅 GPIO13、阳台 GPIO15、卧室 GPIO16；各自专用状态 topic，温湿度共享 JSON；30 秒采样。成功发布 `{"available":true,"temperature":23.2,"humidity":44}`，失败发布一次 `{"available":false}`。
- RSSI：独立 `wifi_rssi` 任务，60 秒采样且网络事件提前唤醒；无有效值发布 `unavailable`。
- 现有 HA 实体：6 个 DHT 温湿度实体及 1 个 Wi-Fi RSSI 诊断实体。

## 4. 正在开发的功能

- 当前没有正在实施的新功能；下一项执行器是阳台窗舵机。
- 未来新增传感器应独立组件、独立采样任务、缓存状态，并注册 `ha_discovery` 状态组和实体。
- 首个执行器落地时再设计通用 MQTT 命令路由和非阻塞执行任务。

## 5. 关键技术

- 初始化顺序：`network_init()` → `ha_mqtt_init()` → `ha_discovery_init()` → `sensors_init()` → `diagnostics_init()` → `network_start()`。
- `ha_mqtt` 将连接和 HA 上线转为 `HA_MQTT_EVENT`；`ha_discovery` 监听这些事件。
- `ha_discovery` 创建 6144 字节栈的 `ha_discovery` 专用任务。`sys_evt` 只投递完整同步请求；专用任务构造 Discovery JSON、发布 MQTT，并在上线时执行“全部 Discovery → 全部最新状态”。
- 业务任务调用 `ha_discovery_publish_state_group()` 仅请求异步发布；同一状态组的待处理请求会合并，最终发布最新缓存值。编码回调只能读取缓存，不能读硬件或阻塞。
- DHT/RSSI 缓存分别使用临界区保护；状态通常 retained，控制命令通常不 retained，关键消息 QoS 1。

## 6. 重要文件说明

- `main/main.c`：NVS、默认事件循环与组件初始化顺序。
- `components/network/`：Wi-Fi 状态机、重连和网络事件。
- `components/ha_mqtt/`：MQTT 生命周期、LWT/availability、HA 状态订阅和通用发布。
- `components/ha_discovery/ha_discovery.c`：注册表、工作队列、专用发布任务、Discovery/状态 JSON。
- `components/sensors/sensors.c`：三个 DHT11 的采样、缓存和 JSON 编码回调。
- `components/diagnostics/diagnostics.c`：RSSI 采样、缓存和网络事件唤醒。
- `yaml/esp32-1.yaml`：仅作迁移逻辑参考，含敏感信息，不提交或输出凭据。
- `ESPHome到ESP-IDF迁移需求与实施计划.md`：当前范围、硬件清单、迁移阶段与验收依据。

## 7. 已知问题

- 三个 DHT11 曾出现 phase C 初始化/读取失败；优先检查供电、共地、数据线、上拉电阻和 GPIO 接线。
- 新的专用发布任务已用于规避 `sys_evt` 栈压力，仍需烧录验证 MQTT 连接后的稳定性。
- 完整构建在当前普通终端被 ESP-IDF Python 缺少 `idf_component_manager` 阻断；使用 VS Code ESP-IDF 专用终端或修复环境后验证。
- 串口监视器打不开通常是 COM 口被占用、选错/重枚举，或连接到与日志不同的 USB 接口；当前主控制台为 UART0、115200，USB Serial/JTAG 为辅助控制台。
- Broker 可能保留旧测试开关的 retained topic，需按需手动清理。

## 8. 下一步要做什么？

1. 在正确 COM 口烧录并验证 DHT、RSSI、MQTT/HA Discovery 及重连后的完整同步。
2. 迁移阳台窗舵机：先确定安全脉宽、方向、机械限位与上电策略，再设计 MQTT 命令/状态闭环及 NVS 持久化。
3. 逐个迁移其余 ADC/火焰等传感器；随后实施 OTA 与存储策略。

## 9. 不能改动/需要注意的约束

- 不硬编码、输出或提交 Wi-Fi/MQTT/API/OTA 凭据；不提交 `sdkconfig`、`build/`、`managed_components/` 或含凭据 YAML。
- 禁止直接修改 `managed_components/`；依赖通过 `idf_component.yml` 管理。
- 保持分层：`network` 不含 MQTT 业务；`ha_mqtt` 不含具体硬件逻辑；业务组件不直接发布 MQTT。
- MQTT/ESP-IDF 事件回调不得进行硬件读取或阻塞操作。
- Wi-Fi 掉线不销毁 MQTT Client；由网络层和 ESP-MQTT 自动恢复。
- 操作前检查 `git status`，保留无关改动；尤其 `components/bsp/CMakeLists.txt` 的删除不属于当前任务，不能恢复或覆盖。
